<!DOCTYPE html>
<html>
  <head>
    <title>Peliculas</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href='/stylesheets/styleheader.css'>
  </head>
  <body>
    <!-- start header -->
      <%- include('partials/header'); %> 
    <!-- end header -->
    <h1>Resultados</h1>
    <ul>
        <% movies.forEach(movie => { %>
            <li> <a href="/movies/detail/<%=movie.id%>" class= "ali">
                <%= movie.title%>  <!-- <%=orden%> -->
            </a>
            </li>
            
        <% }); %>
        <% if ( movies.length == 0) { %>   
			<div class="col-12">
				<h2 style="color: rgb(78, 22, 12);">No hay peliculas que coincidan con su búsqueda</h2>
			</div>
        <% } else { }%>                
            
    </ul>
</body>
</html>